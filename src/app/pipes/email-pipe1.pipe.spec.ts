import { EmailPipe1Pipe } from './email-pipe1.pipe';

describe('EmailPipe1Pipe', () => {
  it('create an instance', () => {
    const pipe = new EmailPipe1Pipe();
    expect(pipe).toBeTruthy();
  });
});
